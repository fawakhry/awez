name: Deploy Aawz prototype to GitHub Pages

on:
  push:
    branches: ["main"]
    paths:
      - "prototype/**"
      - "tests/**"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test order review enhancement
        run: |
          node --check prototype/order-review.js
          node tests/order-review.test.js

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Add prototype enhancements
        run: sed -i 's#</body>#<script src="./accessibility.js"></script><script src="./order-review.js"></script></body>#' prototype/index.html

      - name: Upload website
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./prototype

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4

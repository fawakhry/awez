# المساهمة في عاوز

شكرًا لأي مساهمة تحسّن منصة **عاوز**. الهدف هو إبقاء التغييرات صغيرة، واضحة، وقابلة للتحقق قبل الدمج.

## قبل البدء

- ابدأ من أحدث نسخة من `main` وأنشئ فرعًا مستقلًا لكل تحسين.
- راجع الـPull Requests المفتوحة لتجنب تكرار نفس الفكرة أو تعديل نفس الجزء بدون داعٍ.
- لا تضف أسرارًا، مفاتيح API، بيانات دفع حقيقية، أو بيانات عملاء حساسة إلى المستودع.
- حافظ على التغيير مركزًا: مشكلة واحدة أو تحسين واحد في كل PR قدر الإمكان.

## التحقق محليًا

إذا عدّلت ملف JavaScript داخل `prototype/`، افحص الصياغة أولًا:

```bash
node --check prototype/<file>.js
```

ثم شغّل الاختبار المرتبط بالتغيير باستخدام Node.js Test Runner:

```bash
node --test tests/<test-file>.test.cjs
```

وقبل فتح Pull Request شغّل مجموعة الاختبارات الكاملة نفسها المستخدمة في CI:

```bash
node --test --test-concurrency=1
```

ملفات `.github/workflows/test-suite.yml` و`.github/workflows/deploy-pages.yml` هي المرجع النهائي للفحوص الآلية. الـPR الذي يغيّر ملفات التشغيل أو الاختبارات يجب أن ينجح في **Complete test suite**، وأي تغيير يدخل النسخة المنشورة يجب أن ينجح أيضًا في **Test and deploy Aawz prototype** قبل الدمج.

## Pull Request جيد

اكتب وصفًا مختصرًا يوضح:

1. المشكلة أو الفرصة التي يعالجها التغيير.
2. ما الذي تغير فعليًا.
3. طريقة التحقق والاختبارات التي تم تشغيلها.
4. المخاطر أو القيود المعروفة، إن وجدت.

لا تدمج تغييرًا يؤثر في التشغيل قبل نجاح GitHub Actions. إذا كان التغيير يحتاج قرارًا منتجيًا أو يتعامل مع بيانات حساسة أو خدمة مدفوعة، اتركه للمراجعة بدل الدمج التلقائي.
